{
  "name": "joi-mock",
  "version": "1.0.0",
  "description": "🌟 joi-mock - Effortlessly generate mock data based on your Joi schemas 🌟\n\n",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm test",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage --maxWorkers=20% --maxConcurrent=2 --no-cache",
    "test:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand"
  },
  "keywords": [
    "typescript",
    "joi",
    "mock"
  ],
  "author": "Oğuz Vuruşkaner",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "@faker-js/faker": "^7.6.0",
    "jest": "^29.5.0",
    "joi": "^17.9.1",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1"
  }
}
